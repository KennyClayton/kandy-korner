import { useEffect, useState } from "react"; 
import { useNavigate } from "react-router-dom";

// I want to have a simple page displaying text of what React Route elements are doing.  
export const LearnRoutes = () => {

return <>
<h2>What does the Route element do in React?</h2>
<article className="locations">
    <p>
        Using the route element in React requires that we start with a "parent" Route element. The "Routes" element is that parent element. It's like it's declaring, "OK JS, here come the routes, ie file paths, that I want for each module or component on this web page"
    </p>
    
</article>
</>


};
